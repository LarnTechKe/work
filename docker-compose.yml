services:
  webui:
    build: .
    ports:
      - "${WEBUI_PORT:-5040}:5040"
    command:
      - -redis
      - ${REDIS_URL:-redis://localhost:6379}
      - -ns
      - ${NAMESPACE:-enviar}
      - -listen
      - ":5040"
    env_file:
      - .env
